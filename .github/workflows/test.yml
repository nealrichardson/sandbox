name: Test

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: See what we have, set an env var
      run: |
        set -x
        echo Round 1
        echo printenv > test.sh
        sudo -E bash test.sh
        echo Round 2
        VAR1=foo
        export VAR2=bar
        sudo -E bash test.sh
